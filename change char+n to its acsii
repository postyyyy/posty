
#include <bits/stdc++.h>
using namespace std;

int
main ()
{
 /* char c;
  cin>>c;
  cout<<char(c+3);
  */
  int n;
  cin>>n;
  n=n%26;
  string s;
  cin>>s;
  for(int i=0;i<s.length();i++)
  {
      if(s[i]+3>122)
      {
          s[i]=char(219-s[i]+2);
      }
      else
      {
          s[i]=char(s[i]+3);
      }
  }
  cout<<s;
  
  return 0;
}ex-
3
abxy
dedc

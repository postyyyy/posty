#include <bits/stdc++.h>

using namespace std;

int main()
{
    int n;
    cin>>n;
    std::vector<int>v ;
    for(int i=2;i<=n;i++)
{  
    while(n>0)
    {
    if(n%i==0)
    {
        n=n/i;
        v.push_back(i);
    }
    else
    break;
    }
    
    
}
   for(auto it :v)
   cout<<it<<" ";
    return 0;
}
